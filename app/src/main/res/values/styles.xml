<resources>

<!--Base application theme.-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
         <!--Customize your theme here.-->
        <item name="colorPrimary">@color/Green2</item>
        <item name="colorPrimaryDark">@color/Green2</item>
        <item name="colorAccent">@color/Green2</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/animation_in</item>
        <item name="android:windowExitAnimation">@anim/animation_out</item>
    </style>

</resources>